package com.island.models.animals.herbivorous;

import com.island.models.animals.Animal;

public class Herbivore extends Animal {
    public Herbivore(double weight, int maxPopulation, int maxSpeedOfMovement, double maxCanEat) {
        super(weight, maxPopulation, maxSpeedOfMovement, maxCanEat);
    }
}

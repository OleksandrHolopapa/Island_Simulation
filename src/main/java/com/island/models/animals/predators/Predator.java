package com.island.models.animals.predators;

import com.island.models.animals.Animal;

public class Predator extends Animal {
    public Predator(double weight, int maxPopulation, int maxSpeedOfMovement, double maxCanEat) {
        super(weight, maxPopulation, maxSpeedOfMovement, maxCanEat);
    }
}